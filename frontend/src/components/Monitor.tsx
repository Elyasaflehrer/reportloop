export const Monitor = () => (
  <div style={{ flex: 1, display: 'flex', alignItems: 'center', justifyContent: 'center', flexDirection: 'column', gap: 16, color: 'var(--text-2)' }}>
    <div style={{ fontSize: 40, opacity: 0.2 }}>◉</div>
    <div style={{ fontWeight: 600, fontSize: 16 }}>No active broadcast</div>
    <div style={{ fontSize: 14 }}>Start a broadcast from New Broadcast to monitor live responses here.</div>
  </div>
)
